# Aura Protocol Buffers API

**The official Aura protocol buffers API.**

## Rust API

The Rust API resides in the `aura_rust` directory as the `aura_rust` crate.

## Dart API

The Dart API resides in the `aura_dart` directory as the `aura_dart` package.
